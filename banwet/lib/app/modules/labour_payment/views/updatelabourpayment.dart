import 'package:flutter/material.dart';

class UpdateLabourPayment extends StatelessWidget {
  const UpdateLabourPayment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [Text("ghjnk")],
      ),
    );
  }
}
